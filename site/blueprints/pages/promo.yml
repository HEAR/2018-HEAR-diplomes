title: Promo
# preset: page
# icon: 📝

# pages: true

status:
  draft: false
  unlisted: true
  listed: false
files: true


# pages:
#   template: etudiant
#   sortBy: title asc
#   info: "{{ page.prenom }}"

columns:
  detail:
    width: 1/1
    sections:
      content:
          type: fields
          fields:
            annee:
              type: number
              label: Année de diplôme
              min: 2012
              step: 1
              required: true
              default: 2012
              width: 1/2
            files:
              type: files
              template: csv
              label: Fichier CSV
              width: 1/2
              multiple: false
  main:
    width: 1/1
    sections:
      diplomes:
        headline: Diplômés
        type: pages
        template: etudiant
        sortBy: title asc
        info: "{{ page.prenom }}"
        # type: pagetable
        # limit: 25
        # columns:
        #   title:
        #     text: '{{ page.title }}'


  